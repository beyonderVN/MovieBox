<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="chatMessage"  >
        <item name="android:background">@drawable/view_rounded</item>
        <item name="android:padding">8dp</item>
        <item name="android:maxWidth">280dp</item>
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Medium</item>


    </style>
    <style name="chatMessageClient" parent="chatMessage"  >
        <item name="android:layout_marginLeft">8dp</item>
    </style>
    <style name="chatMessageUser" parent="chatMessage"  >
        <item name="android:layout_marginRight">8dp</item>
    </style>
</resources>
